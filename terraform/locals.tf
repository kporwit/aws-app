locals {
  ##################################################
  # GLOBAL
  ##################################################
  name_prefix = "${var.company}-${var.business_unit}-${var.application}-${var.environment}"
}
